# Copier vers .env puis remplir
OPENROUTER_API_KEY=sk-or-v1-XXXX
# Optionnel : limite de requêtes /api/analyze-plan par IP par heure
ARCHIPLAN_RATE_LIMIT=60
# Optionnel : prix au m² par défaut pour /api/estimate-cost
ARCHIPLAN_PRICE_M2=1800
